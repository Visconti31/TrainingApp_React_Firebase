class Exercise {
  constructor(id, name, bodyPart, exerciseType) {
    ;(this.id = id),
      (this.name = name),
      (this.bodyPart = bodyPart),
      (this.exerciseType = exerciseType)
  }
}

export default Exercise
